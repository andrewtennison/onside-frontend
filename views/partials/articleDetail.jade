// ARTICLE TEMPLATES ///////////////////////////////////////////


script#articleDetail.tmpl(type="text/template")
	header
		h1 <%= title %>
		nav
			a.close.button(href="#") close
			span.buttonGroup
				a.share(href="#") share
				a.save(href="#") save
	\<% if(images) { %> <img src='<%= images %>' /> <% } %>
	div <%= extended %>

script#articleDetail_youtube.tmpl(type="text/template")
	\<object style="height: 245; width: 373">
	\<param name="movie" value="https://www.youtube.com/v/<%= videoId%>?version=3&feature=player_embedded&autohide=1&fs=1&iv_load_policy=3&feature=player_embedded"><param name="allowFullScreen" value="true"><param name="allowScriptAccess" value="always">
	\<embed src="https://www.youtube.com/v/<%= videoId%>?version=3&feature=player_embedded&autohide=1&fs=1&iv_load_policy=3&feature=player_embedded" type="application/x-shockwave-flash" allowfullscreen="true" allowScriptAccess="always" width="373" height="245"></object>

script#articleDetail_iframe.tmpl.overlay(type="text/template")
	header
		h1 <%= title %>
		nav
			a.close.button(href="#") close
			span.buttonGroup
				a.share(href="#") share
				a.save(href="#") save
	div.iframeWrap
		\<iframe src="<%= link %>" />

script#articleDetail_twitter.tmpl.overlay(type="text/template")
	header
		h1 Twitter @ (need name)
		nav
			a.close.button(href="#") close
			span.buttonGroup
				a.share(href="#") share
				a.save(href="#") save

	div.pad
		.original
			\<img src="<%= images %>" />
			p <%= content %>
			cite <%= author %>
		//\<input type="hidden" value='<%= original %>' />
		a.reply(href="#") @reply :: check if user is twit auth, if not login and return to here
		
		ul.older
